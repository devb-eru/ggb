# GGB-ERR-2026-0019: 상태·사건·저장 최소 런타임 미구현

> [충돌·오류 관리](../README.md) / [운영 레지스트리](../충돌오류_레지스트리.md)

| 필드 | 값 |
| --- | --- |
| 심각도 | 높음 |
| 상태 | OPEN |
| 작업 상태 | READY |
| 우선순위 | P1 |
| 담당자 | `beru` |
| 목표 마일스톤 | `TECH_01_FOUNDATION` |
| 영역 | production bootstrap·상태·사건·저장·export |
| 기준 문서 | [문서 17](../../17_상태변수_이벤트ID_Godot데이터구조.md), [`docs/data_contract.md`](../../../../../docs/data_contract.md) |
| 영향 파일 | [`game/scenes/main/main.tscn`](../../../../../game/scenes/main/main.tscn), `game/scripts/autoload/`, `game/scripts/systems/`, `game/data/` |
| 예정 검증 ID | `QA-ERR-0019-WRITER`, `QA-ERR-0019-EVENT`, `QA-ERR-0019-SAVE`, `QA-ERR-0019-EXPORT` |

## 문제

기본 실행점은 연습 씬에서 `scenes/main/main.tscn`으로 분리됐지만 현재 main은 엔진 버전 경고만 수행하는 빈 bootstrap이다. `project.godot`에 autoload가 없고 `game/data/`에는 `.gitkeep` 외 실행 Resource가 없다. 다음 production 최소 경로가 아직 존재하지 않는다.

```text
EventManager
→ StateWriter
→ GameState revision
→ SaveManager
```

또한 runtime schema 1의 새 게임·정상 리셋·손상 복구 fixture와 `export_presets.cfg`가 없다. 따라서 V0 경로 검사는 통과해도 진행 상태 소유권, 원자 커밋, 저장·백업·재개와 Windows 실행은 증명되지 않는다.

## 권장 해결안

1. `GameState`, `EventManager`, `SaveManager`를 autoload로 만들고 책임을 문서 17과 동일하게 제한한다.
2. 일반 시스템 `StateWriter`가 등록된 경로·타입·writer 권한·revision·불변식을 검사한 뒤에만 snapshot을 교체하게 한다.
3. 읽기 전용 `EventDefinition` Resource 한 개와 registry를 연결해 단일 사건의 시작→원자 커밋→완료 신호를 검증한다.
4. `schema_version=1`, `design_revision=v0.4-state-r12` 새 게임 fixture를 만들고 tmp→검증→backup→main 교체를 구현한다.
5. 정상 fixture, 손상 primary·정상 backup fixture, future schema 거부 fixture를 자동 실행한다.
6. Windows debug export preset과 재현 명령을 추가하되 인증 정보는 저장소에 넣지 않는다.

## 완료 조건

- [ ] UI·씬이 GameState 내부 Dictionary를 직접 쓰지 못한다.
- [ ] 선언되지 않은 state path와 stale revision 쓰기가 거부된다.
- [ ] 사건 완료의 복수 영구 효과가 하나의 transaction으로 적용된다.
- [ ] 저장 뒤 재로드 snapshot이 동등하고 손상 primary는 backup으로 복구된다.
- [ ] Godot 4.7 headless import·GDScript parse·Resource load가 통과한다.
- [ ] Windows debug export가 깨끗한 사용자 환경에서 실행된다.

## 검증 계획

| 검증 ID | 검사 | 기대 결과 |
| --- | --- | --- |
| `QA-ERR-0019-WRITER` | 허용·금지·stale revision 쓰기 | 허용 transaction만 revision 1회 증가 |
| `QA-ERR-0019-EVENT` | 최소 사건 Resource 실행 | started→committed→completed 순서와 ID 일치 |
| `QA-ERR-0019-SAVE` | schema 1 저장·복구 fixture | snapshot 동등, backup 복구, future schema 보존 |
| `QA-ERR-0019-EXPORT` | Windows debug export | 오류 없이 생성·실행·종료 |

## 변경 이력

| 날짜 | 주체 | 내용 | 상태 |
| --- | --- | --- | --- |
| 2026-08-30 | `Codex` | bootstrap 분리 뒤에도 남은 production 상태·사건·저장·export 기반 부재를 독립 오류로 등록 | OPEN · READY |
